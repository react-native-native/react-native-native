---
title: Installation
description: Set up React Native Native in your Expo project.
---

## Prerequisites

- [Node.js](https://nodejs.org/) 18+
- [Expo CLI](https://docs.expo.dev/get-started/installation/) (`npx expo`)
- **iOS**: Xcode 16+ with Command Line Tools
- **Android**: Android Studio with NDK installed
- **Rust** (optional): `curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh`

## Create a new project

```bash
npx create-expo-app my-app
cd my-app
```

## Add React Native Native

```bash
npx expo install react-native-native ferrum-fabric
```

## Configure native builds

Add the config plugin to your `app.json`:

```json
{
  "expo": {
    "plugins": [
      "react-native-native"
    ]
  }
}
```

## Prebuild

Generate the native projects:

```bash
npx expo prebuild
```

This creates the `ios/` and `android/` directories with React Native Native configured.

## Run on device

```bash
# iOS
npx expo run:ios --device

# Android
npx expo run:android --device
```

That's it. You're ready to write native code.

## Expo compatibility

React Native Native requires a **Development Build** — a custom-compiled version of your app that includes the native runtime. This works on both **physical devices and simulators**.

<Callout type="warn">
**Expo Go is not supported.** Expo Go is a pre-built app that can't load custom native code. You need a Development Build created with `npx expo run:ios` or `npx expo run:android`.
</Callout>

| Environment | Supported |
|---|---|
| Development Build (physical device) | Yes |
| Development Build (simulator/emulator) | Yes |
| EAS Build | Yes |
| Expo Go | No |

If you're used to Expo Go, the switch to Development Builds is a one-time setup — after that the workflow is identical, with the added ability to run native code.

## Next steps

<Cards>
  <Card title="Hello World" href="/docs/getting-started/hello-world" />
</Cards>
