/**
 * Generated by Ferrum C ABI codegen.
 * Do not edit — changes will be overwritten.
 *
 * @generated by GenerateModuleFerrumABI
 */

#import <Foundation/Foundation.h>
#import <React/RCTBridgeModule.h>
#include <hermes_abi/hermes_abi.h>  // must come before Ferrum headers
#import "FerrumABIHelpers.h"
#import "FerrumABIRegistry.h"

#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wundeclared-selector"
#pragma clang diagnostic ignored "-Wobjc-method-access"

// Protocol for NativeNetworkingIOS — tells compiler the method signatures
@protocol Ferrum_NativeNetworkingIOS_ABI
- (void)sendRequest:(NSDictionary *)arg0 callback:(FerrumCallbackBlock)arg1;
- (void)abortRequest:(double)arg0;
- (void)clearCookies:(FerrumCallbackBlock)arg0;
- (void)addListener:(NSString *)arg0;
- (void)removeListeners:(double)arg0;
@end

// NativeNetworkingIOS.sendRequest
static HermesABIValueOrError ferrum_NativeNetworkingIOS_sendRequest(
    void *ctx,
    HermesABIRuntime *abiRt,
    const HermesABIRuntimeVTable *vt,
    const HermesABIValue *thisArg,
    const HermesABIValue *args,
    size_t count) {
  id<Ferrum_NativeNetworkingIOS_ABI> instance = (__bridge id<Ferrum_NativeNetworkingIOS_ABI>)ctx;
  auto arg0 = ferrum_abi_get_object(abiRt, vt, &args[0]);
  auto arg1 = ferrum_abi_wrap_callback(abiRt, vt, &args[1], ferrum_get_js_invoker());
  dispatch_queue_t queue = [(id)instance respondsToSelector:@selector(methodQueue)]
      ? [(id<RCTBridgeModule>)instance methodQueue] : dispatch_get_main_queue();
  dispatch_async(queue, ^{ [instance sendRequest:arg0 callback:arg1]; });
  return ferrum_abi_make_undefined();
}

// NativeNetworkingIOS.abortRequest
static HermesABIValueOrError ferrum_NativeNetworkingIOS_abortRequest(
    void *ctx,
    HermesABIRuntime *abiRt,
    const HermesABIRuntimeVTable *vt,
    const HermesABIValue *thisArg,
    const HermesABIValue *args,
    size_t count) {
  id<Ferrum_NativeNetworkingIOS_ABI> instance = (__bridge id<Ferrum_NativeNetworkingIOS_ABI>)ctx;
  auto arg0 = ferrum_abi_get_number(&args[0]);
  dispatch_queue_t queue = [(id)instance respondsToSelector:@selector(methodQueue)]
      ? [(id<RCTBridgeModule>)instance methodQueue] : dispatch_get_main_queue();
  dispatch_async(queue, ^{ [instance abortRequest:arg0]; });
  return ferrum_abi_make_undefined();
}

// NativeNetworkingIOS.clearCookies
static HermesABIValueOrError ferrum_NativeNetworkingIOS_clearCookies(
    void *ctx,
    HermesABIRuntime *abiRt,
    const HermesABIRuntimeVTable *vt,
    const HermesABIValue *thisArg,
    const HermesABIValue *args,
    size_t count) {
  id<Ferrum_NativeNetworkingIOS_ABI> instance = (__bridge id<Ferrum_NativeNetworkingIOS_ABI>)ctx;
  auto arg0 = ferrum_abi_wrap_callback(abiRt, vt, &args[0], ferrum_get_js_invoker());
  dispatch_queue_t queue = [(id)instance respondsToSelector:@selector(methodQueue)]
      ? [(id<RCTBridgeModule>)instance methodQueue] : dispatch_get_main_queue();
  dispatch_async(queue, ^{ [instance clearCookies:arg0]; });
  return ferrum_abi_make_undefined();
}

// NativeNetworkingIOS.addListener
static HermesABIValueOrError ferrum_NativeNetworkingIOS_addListener(
    void *ctx,
    HermesABIRuntime *abiRt,
    const HermesABIRuntimeVTable *vt,
    const HermesABIValue *thisArg,
    const HermesABIValue *args,
    size_t count) {
  id<Ferrum_NativeNetworkingIOS_ABI> instance = (__bridge id<Ferrum_NativeNetworkingIOS_ABI>)ctx;
  auto arg0 = ferrum_abi_get_string(abiRt, vt, &args[0]);
  dispatch_queue_t queue = [(id)instance respondsToSelector:@selector(methodQueue)]
      ? [(id<RCTBridgeModule>)instance methodQueue] : dispatch_get_main_queue();
  dispatch_async(queue, ^{ [instance addListener:arg0]; });
  return ferrum_abi_make_undefined();
}

// NativeNetworkingIOS.removeListeners
static HermesABIValueOrError ferrum_NativeNetworkingIOS_removeListeners(
    void *ctx,
    HermesABIRuntime *abiRt,
    const HermesABIRuntimeVTable *vt,
    const HermesABIValue *thisArg,
    const HermesABIValue *args,
    size_t count) {
  id<Ferrum_NativeNetworkingIOS_ABI> instance = (__bridge id<Ferrum_NativeNetworkingIOS_ABI>)ctx;
  auto arg0 = ferrum_abi_get_number(&args[0]);
  dispatch_queue_t queue = [(id)instance respondsToSelector:@selector(methodQueue)]
      ? [(id<RCTBridgeModule>)instance methodQueue] : dispatch_get_main_queue();
  dispatch_async(queue, ^{ [instance removeListeners:arg0]; });
  return ferrum_abi_make_undefined();
}


static const FerrumABIBridgeEntry ferrum_NativeNetworkingIOS_bridges[] = {
    {"sendRequest", 2, ferrum_NativeNetworkingIOS_sendRequest},
    {"abortRequest", 1, ferrum_NativeNetworkingIOS_abortRequest},
    {"clearCookies", 1, ferrum_NativeNetworkingIOS_clearCookies},
    {"addListener", 1, ferrum_NativeNetworkingIOS_addListener},
    {"removeListeners", 1, ferrum_NativeNetworkingIOS_removeListeners},
    {nullptr, 0, nullptr}, // sentinel
};

__attribute__((constructor))
static void ferrum_register_NativeNetworkingIOS_bridges(void) {
  ferrum_abi_register_module("Networking", ferrum_NativeNetworkingIOS_bridges);
}

// Dummy ObjC class so -ObjC linker flag forces inclusion of this translation unit
@interface FerrumABIBridge_NetworkingIOSSpec : NSObject @end
@implementation FerrumABIBridge_NetworkingIOSSpec @end

#pragma clang diagnostic pop
