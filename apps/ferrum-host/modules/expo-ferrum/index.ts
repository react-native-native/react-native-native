/**
 * Ferrum — TurboModule acceleration for React Native.
 *
 * All modules are automatically accelerated via the native
 * Object.defineProperty trap. No JS-side opt-in required.
 *
 * For hot paths, use global.__ferrumGetModule('ModuleName')
 * to get a direct FFI module without RN's JS wrapper overhead.
 */
