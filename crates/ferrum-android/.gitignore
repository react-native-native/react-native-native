# Rust build artifacts
/target/

# Android build artifacts
android/app/build/
android/.gradle/
android/local.properties

# Rust .so files copied to jniLibs (generated, not committed)
android/app/src/main/jniLibs/

# Hermes .so (downloaded, not committed — see NOTES.md §4)
android/app/src/main/jniLibs/arm64-v8a/libhermes.so

# IDE
.idea/
*.iml
