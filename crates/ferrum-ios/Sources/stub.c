/*
 * stub.c — exists solely to give Xcode a compilation unit so it invokes the
 * linker and pulls in libferrum_ios.a (which provides the real main()).
 * No code here; the entry point is in the Rust static library.
 */
